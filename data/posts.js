/* Blog posts — single source of truth for the Blog listing.
   Empty array => the Blog page shows an "under construction" state.
   To add a post: (1) copy /templates/post.html to
   /blog/<slug>/index.html and write it, then (2) prepend an entry
   here. Newest first. See README.md. */
window.POSTS = [];
